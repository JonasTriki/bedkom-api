import csrf from "csurf";

// export const csrfProtection = csrf({cookie: true});

// Used by users /login auth endpoint
// export const csrfTokenPost = csrf({cookie: true, ignoreMethods: ["POST"]});
